{
  "name": "pfms-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "migrate": "node scripts/run-sql.mjs scripts/migrate.sql && node scripts/run-sql.mjs scripts/migrate-002-sap.sql && node scripts/run-sql.mjs scripts/migrate-003-planning.sql",
    "seed": "node scripts/run-sql.mjs scripts/seed.sql",
    "db:setup": "npm run migrate && npm run seed"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^2.0.0",
    "pg": "^8.12.0",
    "xlsx": "^0.18.5"
  }
}
